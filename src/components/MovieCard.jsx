import "../assets/styles/movies.css";

// create a MovieCard component
const MovieCard = ({ movie }) => {
  // destructure the movie object
  // const {title, poster_path, overview, vote_average} = movie1;

  return (
    <div className="movie">
      <div>
        <p>{movie.Year}</p>
      </div>
      <div>
        <img
          src={
            movie.Poster !== "N/A"
              ? movie.Poster
              : "https://via.placeholder.com/400"
          }
          alt={movie.Title}
        />
      </div>

      <div>
        <span>{movie.Type}</span>
        <h3>{movie.Title}</h3>
      </div>
    </div>
  );
};

export default MovieCard;
